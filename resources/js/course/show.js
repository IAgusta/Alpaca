// resources/js/course/show.js
import { toggleProgress, updateCourseProgressUI, toggleAllModules } from './progress';

// Make functions available globally if needed (for inline event handlers)
window.toggleProgress = toggleProgress;
window.updateCourseProgressUI = updateCourseProgressUI;
window.toggleAllModules = toggleAllModules;